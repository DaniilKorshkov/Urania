Following root scripts are implemented:

1) PPM calculator. This script inputs raw RGA scan and calibration parameters as CSV (???) and outputs PPM's of gases as CSV
2) RGA calibration. This script inputs raw RGA scan and calibration parameters as CSV (???) and outputs PPM's of gases as CSV
3) Plot generator
4) Data saver. This script saves data, produced by RGA and PPM calculator to .ROOT object