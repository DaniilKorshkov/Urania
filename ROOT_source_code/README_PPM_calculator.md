PPM calculator runs as compiled binary; inputs raw RGA scan and calibration data as CSV, outputs PPM's of the gases in mixture:

1) Initial M/Z ; M/Z step ; amount of steps ; every entry of raw RGA scan ; every calibration parameter (???) inputed as space separated doubles