Calibration calculator runs as compiled binary; inputs raw RGA scan and mixture composition, outputs calibration parameters of the gases in mixture:

1) Initial M/Z ; M/Z step ; amount of steps ; every entry of raw RGA scan and concentration of every compound in mixture are inputed as space separated floats
2) Raw RGA data is organized into array
3) 
4) Calibration coefficients are exported as space separated values

This program is manually executed on a maintanence (weekly?) schedule and obtained values are appended to config